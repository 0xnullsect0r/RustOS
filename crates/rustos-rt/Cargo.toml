[package]
name = "rustos-rt"
version = "0.1.0"
edition = "2021"
description = "Rust userspace runtime for RustOS — provides _start, syscall wrappers, and the rustos target spec"
license = "MIT OR Apache-2.0"

[lib]
name = "rustos_rt"
crate-type = ["staticlib"]

[features]
default = []
